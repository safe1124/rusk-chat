// 환경 설정 관리 (백엔드 전용)class EnvironmentConfig {    constructor() {        this.isProduction = this.checkProductionEnvironment();        this.backendUrl = 'https://rusk-chat-9mch.vercel.app'; // 실제 Vercel 백엔드 URL    }        // 프로덕션 환경 확인    checkProductionEnvironment() {        return window.location.hostname !== 'localhost' &&                window.location.hostname !== '127.0.0.1' &&               window.location.hostname !== '';    }        // 항상 백엔드 사용    shouldUseBackend() {        return true;    }        // 보안 경고 없음 (백엔드 사용)    showSecurityWarning() {        return null;    }}// 전역 환경 설정 인스턴스const envConfig = new EnvironmentConfig();